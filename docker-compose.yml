services:
  redis:
    image: redis:7.2.4-alpine
    container_name: rebank-redis
    ports:
      - "6380:6379"
    command: redis-server --requirepass ${REDIS_PASSWORD}
    volumes:
      - ./redis_data:/data